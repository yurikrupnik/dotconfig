apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: api-server

namespace: default

resources:
- deployment.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize

images:
- name: ghcr.io/yurikrupnik/api-server
  newTag: latest
